import React from 'react'
import { Text, View } from 'react-native'
import Styles from './styles'

const CameraPhotoStepsComponent = ({ active }) => (
  <View style={Styles.container}>
    <Text>1, 2 y 3</Text>
  </View>
)

export default CameraPhotoStepsComponent
